/**
 * Class name : Main
 * Description : Classe de chargement du jeu
 * @author Jean-François Giammari
 */

public class Main {
    public static void main(String[] args) {

        // Initialisation des


        World w1 = new World("Test",MapStyle.FLAT, 150, 0, 0 ,0 ,0,0);
        // to strign w1
        // change name
        // get name w1
        // Add joueur to w1
        // View World
        // Joueur get position
        // joueur avancer & droite, et gauche (2)
        // Joueur get position
        // Joueur views inventaire
        // WOrld . getBlock(Position)
        // Joueur. mineBlock(Position)
        // Joueur views inventaire
        // Joueur. placeBlock(Position)
        // Joueur vies inventaire
        // new Item (Sword)
        // Joueur. addItem

        // new Mob
        // Joueur get Position
        // World AddEntity (Position, Mob)
        // World. getEntities(id, position, etc...)
        // mob.getstats
        // Joueur.attack(position) // Succes
        // mob.get stat
        // Joueur.getStat
        //mob ; attack (position)
        //Joueur.getSTat





        System.out.println(w1);
    }
}
